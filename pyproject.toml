[project]
name = "fpl-data"
version = "0.1.0"
description = "Personal Premier League / FPL ingest: immutable raw snapshots and DuckDB marts."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "httpx>=0.28.1",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
]

[tool.uv]
package = false
